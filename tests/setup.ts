// Use the Vitest-specific entry point so jest-dom matchers (toBeInTheDocument, etc.)
// augment Vitest's Assertion interface rather than Jest's Matchers interface.
import "@testing-library/jest-dom/vitest";
